// Scheme index file
// 04/20/00
//
// The numbers after the # sign point into the SCHEMELIST.MES file.
//
// When parsing the SchemeList.MES file, it assumes that all the elements from
// +0 to +99 (relative to the # sign below) belong to that scheme.  Therefore
// you must space them out by at LEAST 100 elements.
//

//Music
{0}{<<NONE>>}
